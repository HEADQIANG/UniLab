# @package _global_
defaults:
  - /task/go2_joystick_flat/mixed
  - _self_

training:
  sim_backend: drake
  device: cpu
  no_play: false
  play_render_mode: auto

algo:
  runtime_resolver: null

env:
  drake_backend_mode: batch
  drake_nthread: 0
